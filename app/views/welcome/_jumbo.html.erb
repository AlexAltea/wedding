<section id="welcome-index-leader" class="jumbotron text-center">
  <nav style="text-align: center">
    <%= link_to 'English', url_for(locale: 'en'), class: "language-link" %>
    <%= link_to 'Español', url_for(locale: 'es'), class: "language-link" %>
    <%= link_to 'Deutsch', url_for(locale: 'de'), class: "language-link" %>
  </nav>
  <div class="container">
    <p class="wedding-invite">
      <%= t :wedding_invite %>
    </p>
    <h1 class="jumbotron-heading"><%= t :wedding_couple_names %></h1>
    <p class="leader-date">
      <%= t :wedding_date %>, <%= t :wedding_location %>
    </p>
    <p>
      <%= link_to t(:rsvp_today), new_guest_path, class: 'btn btn-primary wedding-cta-btn' %>
    </p>
  </div>
</section>
