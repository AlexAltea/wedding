<section id="welcome-index-wedding-plan-leader" class="jumbotron text-center">
  <div class="container">
    <h1><%= t :the_plan %></h1>
  </div>
</section>
<section id="welcome-index-wedding-plan">
  <div class="container">
    <div class="row">
      <div class="offset-md-3 col-md-6">
        <h2><%= t :the_plan_timeline_header %></h2>
        <% t('the_plan_timeline_data').each do |item| %>
          <p><%= raw item %></p>
        <% end %>
        <h2><%= t :the_plan_menu_header %></h2>
        <% t('the_plan_menu_data').each do |item| %>
          <p><%= raw item %></p>
        <% end %>
        <h2><%= t :the_plan_rsvp_header %></h2>
        <% t('the_plan_rsvp_data').each do |item| %>
          <p><%= raw item %></p>
        <% end %>
      </div>
    </div>
  </div>
</section>
